import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function AddPage() {
  return (
    <PlaceholderPage title="Add a Book" message="Book intake will be added in a later phase." />
  );
}
