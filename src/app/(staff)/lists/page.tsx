import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function ListsPage() {
  return (
    <PlaceholderPage
      title="Reading Lists"
      message="Shared reading lists will be added in a later phase."
    />
  );
}
