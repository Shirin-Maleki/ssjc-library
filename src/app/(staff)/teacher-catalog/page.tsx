import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function TeacherCatalogPage() {
  return (
    <PlaceholderPage
      title="Teacher Catalog"
      message="This will open a shared, read-only spreadsheet view of the catalog once it's connected in a later phase."
    />
  );
}
